import 'package:flutter/material.dart';

class Origin extends StatelessWidget {
  const Origin({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Origin point"),),
      body: ListView(
        children: [
          
        ],
      ),
      
    );
  }
}